# AGENTS.md

## 專案概述

這是一個為**花蓮高商高一學生**製作的個人學習素材累積網頁工具,搭配一場 90 分鐘的學習歷程檔案講座使用。目標是**讓學生在離場後有一個低門檻的入口,能持續累積零碎素材,並在需要時直接匯出成可上傳到學檔系統的 PDF**。

這不是另一個學檔平台,也不是替代教育部的學習歷程檔案系統。它是一個**前置的累積工具**——學生在這裡累積零碎、粗糙的素材,等到學期末或學年末要正式上傳時,從這裡匯出 PDF,直接送到學檔系統。

開發這個工具請以「**穩定、簡單、無門檻**」為最高原則。被動學生只要看到一個複雜介面就會關掉。如果功能與簡單之間衝突,選簡單。

---

## 必讀資料

### 講座簡報

**請完整讀過這份簡報後再開始開發**:`1150510學習歷程檔案高一講座.pptx`

特別注意以下幾頁,這些是工具的訊息根源:

- **第 18 頁**:拚精美 vs 先求有的對比
- **第 19 頁**:單押精美的風險
- **第 20 頁**:平常都要累積(普通科、藝能科、專業科目、多元表現)
- **第 24 頁**:課程學習成果——三件事:**我學到 / 有困難 / 未來規劃**
- **第 25 頁**:多元表現——三件事:**我做了 / 我感覺 / 我學到**
- **第 26 頁**:能力分類(專業能力與學習態度、解決問題與自我精進、溝通協調與團隊合作)
- **第 55-58 頁**:AI 恐怖谷效應的警告
- **第 61 頁**:不是做好的才上傳,**上傳就是做好**

工具的所有設計選擇都應該回應這些訊息。如果某個功能會讓學生變得「**等做好才開始**」,該功能就違反訊息哲學,應該調整或砍掉。

### 學生背景

- **學校**:花蓮高商,職業類科學校
- **科別**:商業經營、會計事務、資料處理、國際貿易、應用外語、廣告設計
- **年級**:高一
- **使用情境**:90 分鐘講座結束後,在家用手機或電腦打開
- **技術能力**:會用 Line、IG、TikTok,但不會用任何「需要學習」的軟體。任何超過三個動作的流程都會放棄

### 升學脈絡

學生未來會用這些素材去申請四技二專。多數會走「甄選入學」管道(佔總名額 64%),需要把學檔上傳到教育部「全國學生學習歷程檔案系統」。上傳格式是 **PDF(限 4MB)** 或影音檔(限 10MB),搭配 **100 字內簡述**。

工具產出的 PDF 必須能直接通過這個 4MB 限制,且**內容應該包含學生的三件事內容,並可選擇性附加照片**。

---

## 核心訊息哲學(極為重要)

工具的每一個設計選擇都要服務這些訊息。違反這些原則的設計會讓工具失敗。

### 必須做到的

1. **降低累積的門檻** —— 從打開到寫完一筆素材的過程,不能超過 60 秒。
2. **接受粗糙** —— 學生寫一句話也算數,不要強制完整或字數下限。
3. **三件事框架** —— 課程與多元表現各有自己的三件事,但都是引導學生輕鬆動筆,不是學術反思。
4. **離線優先** —— localStorage 為主,沒有後端,沒有登入。
5. **資料是學生的** —— 隨時可匯出、可備份、可清空,不綁定平台。
6. **匯出後可直接上傳** —— PDF 必須符合學檔系統限制(4MB 內、可閱讀、不被當垃圾檔退回)。

### 嚴格禁止的

1. **不要 AI 自動寫作** —— 工具絕對不能有「AI 幫你潤稿」、「AI 幫你補完」、「AI 自動生成反思」這類功能。這直接違反簡報第 55-58 頁的恐怖谷論述,做了等於否定整場講座。
2. **不要登入註冊** —— 任何要學生留 Email、設密碼、綁帳號的設計都會勸退被動學生。
3. **不要強制必填** —— 三件事可以只寫一件,標題可以是一個詞,日期會自動帶。讓學生「先寫了再說」。
4. **不要用「優秀」、「精美」、「完整」當作評價語言** —— 工具的所有提示文字、按鈕文案、提醒訊息,都不能暗示「累積得多 = 厲害」。改用「累積中」、「持續記錄」、「已存」這類中性語言。
5. **不要做花俏的動畫與遊戲化過度** —— 適度的累積里程碑可以,但不要做成解鎖徽章、寵物養成、競賽排名這類元素,這會把訊息焦點從「真實累積」轉成「玩遊戲」。

---

## 使用場景(實際情境)

### 情境 1:晚上想到一件事

學生剛上完商概實作課,回家路上想到「今天那個店面陳列的案例好像可以記」。打開工具,點「+」,選「課程學習成果」,科目選「商概」,標題寫「店面陳列觀察」,三件事寫:
- 我學到:看到的真實店面跟課本講的動線不一樣
- 有困難:不知道該寫多少才夠
- 未來規劃:下次去誠品看看

存。離開。整個過程 90 秒。

### 情境 2:寫完報告想存進來

商業簡報課的期末報告剛交,學生想把這份報告留下來變學檔素材。打開工具,新增一筆「期末簡報:咖啡店行銷策略」,三件事寫完,**附加照片**(他做的簡報截圖 3 張),儲存。

到了學期末要交檔時,點「匯出 PDF」,工具產出一份包含三件事文字 + 3 張簡報截圖的 PDF,大約 1-2 MB,直接上傳到學檔系統。

### 情境 3:回頭找之前寫過什麼

學生兩個月後想知道自己累積到哪裡,打開工具,看到時間軸上有 12 筆素材。可以依「課程 / 多元表現」、「科目 / 類別」篩選。點任一筆可以編輯、補充、匯出。

### 情境 4:換手機 / 清快取

學生換新手機,擔心資料消失。工具有「全部匯出」(JSON 備份)。新手機打開工具,「匯入備份」即可還原。

---

## 核心功能規格

### 1. 新增素材

點擊「+」按鈕進入新增介面。

**欄位**:
- **類型**(必選):課程學習成果 / 多元表現
- **標題**(必填,最多 30 字):一句話描述
- **科目或項目**(下拉選單)
  - 課程學習成果可選:國文、英文、數學、商概、會計、計概、實習科目、專業科目、其他
  - 多元表現可選:幹部經歷、競賽、證照、服務學習、彈性學習、社團、職場學習、作品成果、其他
- **三件事**(三個獨立文字欄位,**全部選填,不強制必填**):
  - 課程學習成果類型時:**我學到 / 有困難 / 未來規劃**
  - 多元表現類型時:**我做了 / 我感覺 / 我學到**
  - 欄位的 placeholder 提示語要口語、低門檻,例如「我學到」的 placeholder 是「寫一句你看見的、想到的、學會的」,不要寫成「請描述你的學習成果與反思」
- **附加照片**(可選):支援多張,但要警告學生太多照片會讓 PDF 超過 4MB
- **備註**(選填):URL 連結、補充說明
- **日期**:自動帶入,可手動修改

**儲存後**:回到主畫面,顯示「已存」短暫提示後消失。**不要跳出對話框、不要強制學生看任何訊息**。

### 2. 素材列表(主畫面)

打開工具看到的第一個畫面就是素材列表。

**頂部**:
- 累積筆數(例:「已累積 12 筆」)
- 「+ 新增素材」按鈕(明顯)
- 今日提示(可選功能,見下文)

**中段**:篩選列
- 全部 / 課程學習成果 / 多元表現
- 二級篩選:依科目或類別

**列表**:時間軸由新到舊,每一筆顯示
- 標題、類型標籤、科目、日期
- 點擊展開看三件事內容
- 編輯、刪除、匯出 PDF 按鈕

### 3. 今日提示(可選但推薦)

降低空白頁恐懼。每次打開工具,首頁顯示一句提示。例如:
- 「今天有沒有哪一刻你想記下來?」
- 「上一週你做過什麼小事是別人不知道的?」
- 「最近你跟誰討論過什麼有趣的事?」
- 「你最近卡在什麼地方?寫下來等於前進一步。」

請至少設計 10 句,隨機輪流出現。**不要用勵志雞湯**(例如「相信自己」、「夢想成真」這類),要口語、降門檻。

### 4. 匯出 PDF(本工具的關鍵功能)

學生選一筆素材,點「匯出 PDF」。產出的 PDF:

- **內容**:標題、日期、科目、三件事完整內容、學生附加的照片
- **格式要求**:
  - **可複製文字**(教授審查時可選取、可搜尋)
  - **支援中文**(必須嵌入中文字型,不要讓學生開啟後變方塊)
  - **單檔大小不超過 4MB**(學檔系統限制)
  - **A4 直式**,排版乾淨,**不要在這份 PDF 上放任何網頁底圖、品牌標誌、工具浮水印**(學生上傳的是給大學審查用的)
- **建議套用工具**:`pdfmake`(可保留可複製文字 + 支援中文嵌入字型 + 圖片插入)
  - 但 Codex 可評估其他方案,只要同時滿足「可複製文字 + 中文 + 圖片」三項即可
  - 字型建議:Noto Sans TC 或 Source Han Sans TC,只嵌入常用字元 subset 控制檔案大小

### 5. 全部備份與還原

- **匯出 JSON**:把所有素材打包成單一 JSON 檔案下載
- **匯入 JSON**:讓學生從備份還原(覆蓋現有資料前必須確認)

### 6. 累積里程碑(輕度遊戲化)

第 1、10、30、50 筆累積時,出現短暫鼓勵訊息。**訊息要中性、不要過度勵志**:

- 第 1 筆:「第一筆素材完成。持續累積。」
- 第 10 筆:「累積 10 筆了。」
- 第 30 筆:「30 筆。三年後的你會謝謝你現在的累積。」
- 第 50 筆:「50 筆。素材庫成形了。」

訊息一閃而過(3 秒),不要強迫學生點擊或關閉。

---

## 不要做的事(YAGNI 邊界)

請 Codex 嚴格遵守這個清單,**不要為了「看起來功能更完整」而擴大範圍**。

1. **不要做帳號登入系統** —— 純前端,localStorage 即可
2. **不要做後端** —— 沒有 Node.js server、沒有資料庫、沒有 API
3. **不要做 AI 輔助寫作** —— 違反訊息哲學
4. **不要做雲端同步** —— 學生用 JSON 匯出/匯入處理跨裝置
5. **不要做社群功能** —— 不要分享、按讚、留言、排行榜
6. **不要做美化 PDF 模板選擇** —— 一個乾淨模板就好,模板選擇會讓學生陷入裝飾糾結
7. **不要做語音輸入** —— 過度功能,瀏覽器原生語音輸入夠用
8. **不要做學檔系統真實上傳** —— 工具產 PDF,學生自己去學檔系統上傳。不要嘗試介接教育部系統(會違反個資與系統穩定)
9. **不要做付費功能、廣告、追蹤** —— 完全免費、無分析、無 cookie
10. **不要做動畫過場與酷炫轉場** —— 簡單即時的回應就好

---

## 資料模型

### localStorage 結構

```javascript
// key: 'portfolio_items'
// value: 陣列,每個元素是一筆素材

{
  id: 'string (UUID 或時間戳)',
  type: 'course' | 'multi',  // 課程學習成果 / 多元表現
  title: 'string',
  subject: 'string',  // 科目或類別
  threeThings: {
    // 課程: learned / difficulty / plan
    // 多元: did / felt / learned
    field1: 'string',
    field2: 'string',
    field3: 'string'
  },
  photos: [],  // 照片處理見下方
  note: 'string',  // 備註,可放網址
  createdAt: 'ISO timestamp',
  updatedAt: 'ISO timestamp'
}
```

### 照片儲存(留給 Codex 解決的技術問題)

照片如果都存進 localStorage 會爆容量(localStorage 上限 5-10MB)。請 Codex 評估以下方案,選擇最穩定、最不會壞掉的:

**選項 A**:照片用 IndexedDB 儲存(容量大、與 localStorage 分開),素材紀錄用 localStorage,兩者用 ID 關聯。
**選項 B**:照片不長期儲存,只在「匯出 PDF」當下讓學生選擇照片,即時插入 PDF 後就丟掉。
**選項 C**:其他混合方案。

**選擇的考量點**:
- 學生的儲存空間不該被工具吃掉太多
- 照片遺失對學生衝擊不能太大(需要警告或備份機制)
- IndexedDB 在某些瀏覽器隱私模式下會失效,要有 fallback

**不論選哪個方案,都要在 UI 明確告訴學生「照片儲存方式」與「風險」**(例如「照片只存在這台裝置,換手機請先匯出備份」)。

---

## 視覺設計

### 整體方向

**底圖貫穿全站,但匯出檔案乾淨**(這是老師的決定,不要更動)

老師會提供底圖檔案。底圖的處理方式:

- **首頁、列表頁、新增頁、編輯頁**:底圖作為背景,降低飽和度與透明度(建議 alpha 0.15-0.25),確保前景文字清晰可讀
- **匯出 PDF**:**完全乾淨,不要任何底圖、不要工具標誌、不要浮水印**。PDF 是學生要送出去給大學的,不能讓它看起來「像從某個工具產出來的」
- **手機優先**:多數學生會用手機打開,RWD 必須做好,觸控目標夠大

### 配色

底圖會決定主視覺氛圍。其他配色建議:

- 主色:**從底圖萃取一個飽和度適中的色彩**,作為按鈕、強調元素
- 背景:**米白或極淺色**(不要純白,純白配底圖會太刺眼)
- 文字:**深墨色**(不要純黑,純黑顯得僵硬;建議 #2C2A26 或類似的暖墨色)
- 警示色:**柔和的暖橘**(用於提醒、警告,但不要紅色,紅色太緊張)

### 字體

- **中文**:Noto Sans TC,字重至少包含 Regular 與 Bold
- **英文與數字**:Inter 或系統預設無襯線
- **程式碼或網址**:JetBrains Mono 或 monospace
- **避開**:書法體、文青風手寫體、過度華麗的標題字體

### 介面語氣

所有按鈕、提示、錯誤訊息的文字風格:

- **必須**:口語、簡潔、像老師寫的
- **不要**:行銷文案口吻、AI 助理風格、過度禮貌
- **範例**:
  - ✓ 「已存」
  - ✗ 「您的素材已成功儲存!」
  - ✓ 「這筆要刪掉嗎?」
  - ✗ 「您確定要永久刪除此筆紀錄嗎?此操作無法復原。」
  - ✓ 「照片太多,可能超過 4MB 上限」
  - ✗ 「警告:檔案大小可能超出限制」

---

## 技術選型建議

### 框架

**建議選一**:
- **Vanilla JS**(最輕量,完全離線,部署簡單)
- **Vue 3 with CDN**(輕量、容易上手)
- **Alpine.js**(極輕量,適合小工具)

**不建議**:
- React with build step(部署複雜化)
- Next.js / Nuxt(過度,這是純前端工具)

老師偏好簡單、可長期維護的方案,**選 Vanilla JS 或 Alpine.js 較合適**。但 Codex 可依實作品質判斷。

### PDF 生成

**首選**:`pdfmake` —— 滿足「可複製文字 + 中文 + 插圖」三大需求

如果 Codex 認為其他方案更穩定可行(例如 jsPDF + 中文字型 plugin),請在交付的 README 中說明選擇理由。

### 照片處理

見上文「資料模型」章節。Codex 自行評估三個方案後選最穩定的。

### 部署目標

- **GitHub Pages**(老師熟悉、免費、穩定)
- 必須完全靜態,不依賴任何後端服務

### 瀏覽器支援

- **必須支援**:Chrome、Safari、Firefox 最近兩年版本(桌面 + 手機)
- **不必支援**:IE、Opera Mini

---

## 互動細節

### 開啟流程

第一次打開工具:

1. 顯示一段歡迎訊息(不超過三句話),解釋這是什麼、怎麼用
2. 第一個 CTA:「+ 新增第一筆素材」
3. **不要強制看完教學影片、不要彈出歡迎對話框、不要要求填寫個人資料**

第二次以後打開:

1. 直接顯示素材列表
2. 頂部今日提示(若有)
3. 隨時可點「+」新增

### 編輯流程

1. 點擊任一筆素材,展開
2. 點「編輯」進入編輯模式
3. 所有欄位可改
4. 「儲存」回到列表,顯示短暫「已更新」提示

### 匯出 PDF 流程

1. 在素材列表或展開的素材上點「匯出 PDF」
2. **若該筆有照片**:跳出選照片介面,讓學生選要附上哪些
3. **若該筆沒有照片**:可選擇從相簿臨時加照片,或直接匯出純文字 PDF
4. 工具產出 PDF,學生可預覽或直接下載
5. 下載後,工具顯示一段說明:「這個 PDF 可以上傳到學檔系統。檔案大小 X MB(限制 4MB)。」

### 刪除流程

1. 點「刪除」按鈕
2. 簡短確認(一行字 + 兩個按鈕,不要彈跳大對話框)
3. 刪除即時生效,顯示「已刪除」短暫提示
4. **不做「垃圾桶」、「30 天後永久刪除」這類功能**

---

## 留給工程師判斷的問題(請 Codex 主動處理)

老師相信工程師擅長解決特定技術問題,以下幾點請 Codex 自行決定方案,並在交付的 README 說明選擇理由:

1. **PDF 生成的具體 library 與字型 subset 策略**(目標:可複製文字 + 中文 + 插圖 + 4MB 內)
2. **照片儲存的方案選擇**(localStorage / IndexedDB / 不長期儲存)
3. **跨瀏覽器相容性的 fallback 設計**(隱私模式、舊版 Safari、容量爆滿)
4. **效能優化**(素材累積到 100 筆以上時,列表是否需要虛擬捲動)
5. **錯誤處理**(localStorage 寫入失敗、PDF 生成失敗、照片讀取失敗等情境的處理流程)

請工程師發揮所長,選擇能讓工具長期穩定運作的方案。

---

## 交付清單

請產出以下檔案:

1. **`index.html`**(主介面,含所有頁面與互動)
2. **`assets/`**:CSS、JS、字型、底圖等資源(若有分檔)
3. **`README.md`**:
   - 部署說明(GitHub Pages 一鍵部署步驟)
   - 技術選型理由(特別是 PDF library 與照片儲存方案)
   - 已知限制與注意事項
   - 維護指引(老師日後想加功能時可以從哪裡改)
4. **`CHANGELOG.md`**:版本紀錄,方便追蹤改動

老師會自己部署到 GitHub Pages,部署後產生 QR Code 印在講座的最後一頁。

---

## 驗收標準

工具完成後,要能通過以下檢查:

1. **第一次打開到完成第一筆素材,流程不超過 60 秒**
2. **所有按鈕與提示文字符合「介面語氣」章節的要求**(中性、口語、不勵志)
3. **任意一筆素材都能匯出成可上傳的 PDF**(可複製文字、中文正常、4MB 內)
4. **完全離線可用**(關掉網路依然能新增、編輯、匯出 PDF)
5. **手機與桌面都可用,RWD 不破版**
6. **任何 AI 自動寫作功能都不存在**
7. **底圖在工具介面出現,但 PDF 是乾淨的**
8. **JSON 匯出與匯入可以正常往返**

---

## 老師背景補充(供 Codex 參考工具語氣)

老師是花蓮高商國文科教師,長期推動 AI 與教育整合,曾任教務主任,熟悉前端開發(HTML/CSS/Tailwind/React、Python、Firebase、GitHub Pages),對工具的判斷標準是 **KISS / DRY / YAGNI**——簡單優先、不重複造輪、不做用不到的功能。

老師的講座核心訊息是「**先求有,再求好**」與「**上傳就是做好**」。這個工具是這個訊息的物質化——讓學生真的能「先求有」、真的能「上傳」。如果工具的某個功能讓學生變得「**等做好才開始**」,就違反了這個訊息,該功能就應該砍掉或重新設計。

工程上請以「能用三年不壞」的標準做。老師會交給未來幾屆學生使用,不會頻繁更新。
